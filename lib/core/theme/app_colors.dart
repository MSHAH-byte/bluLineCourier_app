import 'package:flutter/material.dart';

/// Design system color palette for BluLine Courier
class AppColors {
  AppColors._();

  // Background Colors
  static const Color primaryBackground = Color(0xFF0B131F);
  static const Color secondaryBackground = Color(0xFF161F30);
  static const Color surface = Color(0xFF1E293B);

  // Accent & Interactive Colors
  static const Color primaryAccent = Color(0xFFF5A623);
  static const Color interactiveBlue = Color(0xFF1D4ED8);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textDisabled = Color(0xFF64748B);
  static const Color textAccent = Color(0xFFF5A623);

  // Semantic Colors
  static const Color success = Color(0xFF10B981); // Standard success green
  static const Color warning = Color(0xFFF59E0B); // Standard warning orange
  static const Color error = Color(0xFFEF4444);   // Standard error red
  static const Color info = Color(0xFF3B82F6);    // Standard info blue

  // Border Colors
  static const Color border = Color(0xFF334155);
}
